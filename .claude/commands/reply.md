---
description: 針對某封信草擬回覆
---

請幫我草擬一封回覆。

我會告訴你要回哪封信（寄件者／主旨／或描述）以及大致想表達的內容。

步驟：
1. 用 `search_threads` 找到那封信，用 `get_thread` 讀完整內容與 messageId。
2. 依我的意圖草擬回覆，語氣得體、繁體中文（除非原信是其他語言則配合之）。
3. 用 `create_draft` 建立草稿，帶上 `replyToMessageId`。
4. **只建立草稿，絕不直接寄出。** 建立後把草稿內容貼給我確認。

如果我的意圖不夠清楚，先問我再動手。
